# Basic version of vulnerable webapp for Cyber Security Academy

### USAGE
1. Copy repo
2. a) Run startServer.cmd and specify PORT <br>
   b) "java -jar server.jar \<PORT\>" from terminal in /libs folder
3. localhost:\<PORT\>